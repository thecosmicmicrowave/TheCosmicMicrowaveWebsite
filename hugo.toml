baseURL = "http://localhost:1313/"
title = "FTC 35817 — The Cosmic Microwave"
languageCode = "en-us"
theme = ""   # no theme, we use custom layouts directly

[params]
  # You can add custom params here if needed
  tagline = "Cogitare est coquere — To think is to cook."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true   # allows raw HTML in Markdown (e.g., if you need it)