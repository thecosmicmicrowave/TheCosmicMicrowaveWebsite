baseURL = "https://cosmicmicrowave.org"    # <- replace with your actual domain
title = "FTC 35817 — The Cosmic Microwave"
locale = "en-us"                       # replaces languageCode

[params]
  tagline = "Cogitare est coquere — To think is to cook."

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true