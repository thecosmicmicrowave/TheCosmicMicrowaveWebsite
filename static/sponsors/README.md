# Sponsor logos

Drop sponsor logo files here (SVG preferred, PNG with transparent background
also fine), then reference the filename in `data/sponsors.yaml`. This directory
is served at `/sponsors/<file>`.
